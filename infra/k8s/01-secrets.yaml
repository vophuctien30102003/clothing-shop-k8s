apiVersion: v1
kind: Secret
metadata:
  name: clothing-secrets
  namespace: clothing-ns
type: Opaque
stringData:
  # MySQL credentials
  mysql-root-password: "ClothingRoot@123"
  mysql-app-user: "clothing_app"
  mysql-app-password: "Cl0th1ngApp@123"
  mysql-database: "clothingdb"
  
  # MinIO credentials
  minio-access-key: "minioadmin"
  minio-secret-key: "minioadmin"
  
  # NOTE: In production, use external secret management (Vault, AWS Secrets Manager, etc.)
  # For public repos, use placeholders and inject via CI/CD or external tools

